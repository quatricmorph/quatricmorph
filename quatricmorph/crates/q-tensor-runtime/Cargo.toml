[package]
name = "q-tensor-runtime"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
ndarray = { version = "0.15", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"

[lib]
path = "src/lib.rs"
