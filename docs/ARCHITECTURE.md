# Architecture pointer

**Canonical implementation architecture (do not fork):**

→ [`../ARCHITECTURE.md`](../ARCHITECTURE.md)

That root document is the unchangeable source of truth for Quatricmorph’s tensor visualization and platform implementation architecture. All other docs must defer to it when they conflict.

Historical Three.js / `mm`-migration architecture notes were removed as they conflict with the CesiumJS + tensor-tile design in the root document.
